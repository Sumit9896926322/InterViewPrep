package test;
import java.util.Scanner;

public class Geeks {

	public static void main(String[] args) {
		   Scanner s = new Scanner(System.in);
		   int t = s.nextInt();
		   s.nextLine();
		   while(t-- > 0) {
		       StringBuilder s = new StringBuilder("sumit");
		       
		       for(int i = 0;i<=s.length()/2;i++)
		           s.replace(i,s.length()-i);
		           
		       
		       
		       
		    
	}

}